# Anthropic API key —— 仅 FastAPI 后端使用,绝不进入 Next.js bundle 或浏览器。
# 在 console.anthropic.com 拿到 key 后,在本目录建 .env 文件(已被 .gitignore 排除),内容:
#   ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=

# 可选:覆盖模型名。默认 claude-sonnet-4-6,跟交付包 vedic_interpret.call_claude 一致。
# ANTHROPIC_MODEL=claude-sonnet-4-6
